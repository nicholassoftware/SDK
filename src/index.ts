import * as books from './routes/book';
import * as movies from './routes/movie';
import * as characters from './routes/character';
import * as quote from './routes/quote';

const LOTR = {
    books,
    movies,
    characters,
    quote,
};

export default LOTR;
